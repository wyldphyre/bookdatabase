Flask==3.1.3
Flask-SQLAlchemy==3.1.1
# Pinned though it arrives via Flask-SQLAlchemy: it is the dependency here most
# likely to change behaviour under us, and left unpinned each image rebuild picks
# up whatever is current at that moment.
SQLAlchemy==2.1.0
Werkzeug==3.1.8
requests==2.32.5
beautifulsoup4==4.14.3
gunicorn==22.0.0
Pillow==12.3.0
